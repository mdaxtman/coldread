// Placeholder — Task 15 replaces this with the real fit report view.
import type { FitReport, ResumeVariant } from '../../types'

interface FitReportViewProps {
  jdId: string
  fitReport: FitReport
  resume: ResumeVariant | null
  onGenerate: () => void
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const FitReportView = (_props: FitReportViewProps) => null
