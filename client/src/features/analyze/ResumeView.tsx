// Placeholder — Task 15 replaces this with the real resume view.
import type { ResumeVariant } from '../../types'

interface ResumeViewProps {
  jdId: string
  variant: ResumeVariant
  fitReportId: string
  onRefine: (variantId: string) => void
}

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const ResumeView = (_props: ResumeViewProps) => null
